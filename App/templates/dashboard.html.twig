<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Tableau de bord</title>
</head>
<body>
    <h1>Liste des utilisateurs</h1>

    {% if utilisateurs is empty %}
        <p>Aucun utilisateur inscrit.</p>
    {% else %}
        <table border="1" cellpadding="5" cellspacing="0">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Avatar</th>
                    <th>Date d'inscription</th>
                </tr>
            </thead>
            <tbody>
                {% for u in utilisateurs %}
                    <tr>
                        <td>{{ u.id }}</td>
                        <td>{{ u.nom }}</td>
                        <td>{{ u.email }}</td>
                        <td>
                            {% if u.avatar_url %}
                                <img src="{{ u.avatar_url }}" alt="Avatar" width="40">
                                 <!-- Non échappé → si `avatar_url` n’est pas sûr, possible faille XSS -->
                            {% else %}
                                -
                            {% endif %}
                        </td>
                        <td>{{ u.date_inscription }}</td>
                         <!-- Le format brut de la date peut être illisible → formatage recommandé -->
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</body>
</html>
